# Terraform
.terraform/
.terraform-*/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl

# Environment secrets
.env
.env.local
.env.*.local
*.secret

# Runtime compose override written by scripts/enable-llm-planner.sh (references
# the Anthropic key via ${...}; never holds the value, but keep it out of git).
docker-compose.demo.llm.yml

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Local dev-stack overrides — machine-local worktree paths, never shared.
# See docker-compose.override.yml.example.
docker-compose.override.yml
docker-compose.*.override.yml
